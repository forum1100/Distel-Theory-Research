# DT Hawking Accessibility — Causal Process Ledger

Status: SOURCE-FAITHFUL EXECUTED-CODE RECOVERY / CAUSAL RECONSTRUCTION

This ledger records why each historical code revision existed. It does not flatten the sequence into the final successful parameter set.

`initial behavior -> observed failure -> proposed correction -> changed parameters/mechanism -> new behavior -> next residual`

Scientific boundary: this is a toy horizon/accessibility model. It does not derive Hawking radiation from GR/QFT and does not validate DT physically.

## Run 1 — initial leakage/persistence model
The initial model generated escaped and absorbed flux proxies, but outside persistence memory saturated at the hard maximum (`5.0`) and the entire outside grid retained persistent signal. The visible pattern therefore could not be trusted as discriminating evidence: excessive persistence feedback could manufacture a clean result by saturating the modeled state.

Causal correction generated by Run 1: weaken memory feedback and test the DT terms explicitly rather than trust the headline leakage pattern.## Run 2 — ablation model
The next run introduced explicit ablations for the void term, persistence threshold, and `S+V` transparency term. This was a methodological change: instead of asking only whether the full model produced leakage, the run asked whether removing individual mechanisms changed the outcome.

Failure: all four variants hit the same escaped-flux ceiling (`219`) and the same outside-persistence mass (`300`). Escaped flux was therefore saturated and non-discriminating even though some secondary diagnostics differed.

Causal correction generated by Run 2: remove the saturation regime itself before drawing conclusions from ablations.

## Run 3 — anti-saturation revision
The model weakened persistence gain, reduced diffusion/fluctuation amplitude, increased the threshold, and lowered clipping. This removed the previous ceiling, but overcorrected: Full-DT produced zero modeled horizon events and almost no escaped flux, while removing the threshold produced runaway persistence (`63.35` escaped-flux proxy).

This is a two-sided failure that constrained the next run: the earlier model was too permissive/saturating; this model made the threshold too suppressive. The next change therefore had to reopen the event process without restoring runaway persistence.## Run 4 — threshold/fluctuation retuning
The historical code lowered `gamma0` from `0.08` to `0.018`, raised fluctuation amplitude from `0.028` to `0.05`, and raised persistence gain from `0.015` to `0.02`. This directly answered Run 3's residual by reopening events while retaining a threshold.

The final historical run produced nonzero event/leakage proxies without returning to the Run-1 global saturation regime. The ablations still mattered: no-threshold remained runaway, no-void changed absorption to zero, and removing `S+V` transparency sharply reduced escaped flux.

Replay validation reproduces the final historical numerical outputs to about `2.9e-16` maximum absolute difference. That validates source/execution recovery, not the physical model.

## Causal conclusion
The four-run sequence is not "DT worked." It is: saturation failure -> ablation saturation failure -> anti-saturation overcorrection -> bounded retuning. The final run only has meaning because the three earlier failure states constrain why its parameter changes were made.