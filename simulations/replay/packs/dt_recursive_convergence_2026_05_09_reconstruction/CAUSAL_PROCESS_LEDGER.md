# DT Recursive Convergence — Causal Process Ledger

Status: REPAIRED-RECONSTRUCTION EXECUTION / CONTRADICTORY REPLAY PRESERVED

This pack must be interpreted as a causal process, not as a proof-bearing endpoint. The historical Gmail source is higher-provenance than the repaired executable. The repaired executable exists because line merges in extracted text prevented direct execution.

`historical source -> extraction defect -> explicit repairs -> first execution failure -> environment correction -> successful replay -> contradictory result -> unresolved implementation/scoring questions`

## Source and reconstruction seam
The May 9, 2026 Gmail message is the historical source. PDF extraction merged adjacent tokens/lines, so `simulation.py` is a repaired reconstruction rather than byte-identical historical code. Each repair is separately disclosed in the manifest. The causal reason for repair was executability, not improvement of the theory.

That distinction remains permanent: successful execution of the repaired reconstruction cannot retroactively turn it into original source.## Execution seam
The first replay attempt failed because NumPy was unavailable to the active Python interpreter. That failure is part of the execution lineage. NumPy and pandas were installed into the active interpreter and the same command was rerun.

The corrected environment then completed the 50-trial replay. Under the repaired reconstruction, `fact_only` materially outperformed `hybrid` and `topology_only` on the pack's own score and final mean error; both topology-containing modes hit the 650-cycle ceiling on average.

## Contradictory result as causal evidence
This replay does not support a simple "topology inheritance/hybrid is superior" interpretation. Instead it creates a new residual: either the topology handoff implementation, thresholds, target/scoring construction, or some combination is too conservative or otherwise mis-specified.

That negative result must govern the next test design. A successor run should be prediction-first and isolate reopening/adaptation, topology transfer, target construction, and scoring rather than simply tuning until hybrid wins.

Scientific boundary: convergence toward encoded target corridors is software behavior, not independent evidence for the physical propositions represented by the domain labels.