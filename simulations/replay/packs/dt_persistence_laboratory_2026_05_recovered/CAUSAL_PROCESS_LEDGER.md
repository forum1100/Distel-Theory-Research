# Persistence Laboratory — Causal Process Ledger

Status: `VERBATIM_EXECUTION_CELL_RECOVERED / SOURCE-FAITHFUL REPLAY VALIDATED`

Scientific status: `TOY_SYNTHETIC_PERSISTENCE_MODEL_NOT_DOMAIN_VALIDATION`

Primary source is the May-2026 ChatGPT export, conversation `69fcc4b7-cf9c-83ea-8ff4-45499aecdb98`, execution node `c642dbc9-a8bf-4373-8a53-adb39df771b4`. The recovered code SHA-256 is `a17548e9ee5444f8d062fa3ea096d83e2e6daf939cd4995d2a97327198961522`.

## Causal process

The preceding discussion had already observed that a DT-style persistence loop could generate attractors, hysteresis-like memory, threshold separation, and recursive stabilization, while warning that those behaviors alone do not replace established physics.

The user then asked to set up prediction simulations and pit the assistant against the framework. The assistant proposed an adversarial prediction suite: define DT observables, compare against baselines, and identify where DT adds predictive power versus where it collapses into ordinary nonlinear dynamics.

The execution battery tested five synthetic regimes: threshold persistence under noise, hysteresis after perturbation, predatory constraint monopolization, senescence/lock-in, and a null/random-walk control. Baselines were a linear accumulator and a sigmoid threshold model; the DT-like branch used `I*C*K`, dynamic `gamma0`, recursive reinforcement, and memory overlap.

The run completed successfully and the exact source-faithful replay reproduces all 15 displayed aggregate rows within six-decimal historical display precision.## Validation correction

The strongest historical-looking result is also the most important limitation. The code explicitly says the synthetic `trueS` target is generated by a nonlinear threshold-with-memory process that is “not identical to DT but DT-favored.” Its generator uses the same broad structural ingredients the DTRecursive candidate is designed to model: threshold gating, effective interaction, recursive accumulation, and memory.

Therefore DTRecursive winning this battery is **not independent predictive validation of DT**. It demonstrates that a DT-like model recovers data generated by a structurally similar synthetic process substantially better than the chosen simpler baselines. That is useful implementation evidence and a sanity test, but it is partly generator-model alignment.

The null-regime result does not remove this concern: the null label changes the input perturbation schedule, but the synthetic target is still produced by the same threshold/memory generator. A stronger negative control would generate outcomes from mechanisms not sharing DTRecursive's architecture and preregister the expected failure conditions.

Canonical interpretation: `SOURCE_REPLAY_CONFIRMED / HISTORICAL_NUMBERS_RECOVERED / INDEPENDENT_PREDICTIVE_CLAIM_NOT_ESTABLISHED`.

Next valid successor test: freeze the recovered model, use independently generated or empirical target processes, include matched-complexity nonlinear/recurrent baselines, and measure out-of-sample prediction without retuning after observing results.