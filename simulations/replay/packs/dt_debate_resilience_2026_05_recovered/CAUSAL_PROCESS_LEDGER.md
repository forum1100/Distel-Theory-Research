# Debate / Civilization-Resilience Simulation — Recovered Causal Process

Status: `VERBATIM_EXECUTION_CELLS_RECOVERED / SOURCE-FAITHFUL REPLAY PARTIAL`

Scientific status: `TOY_SOCIAL_SYSTEMS_RESILIENCE_MODEL_NOT_POLICY_OR_SOCIAL_PROOF`

The historical registry label “Debate Simulation” did not occur as an exact source title. Semantic mechanism recovery instead located a four-run policy/resilience chain in conversation `6a012dda-2a50-83ea-b552-0cff43025f4c` whose modeled alternatives match the preserved historical lesson: centralized optimization, laissez-faire/noise, and distributed recursive correction under shocks.

The recovered source is authoritative for what was executed; this ledger is a derived causal index and must not replace the original message graph.## Four-run causal chain

**Run 1 — policy/resilience comparison.** User: “No im saying run the sim from this 1.” The implementation compared `centralized_optimization`, `laissez_faire_noise`, and `distributed_recursive_correction` under common shocks. Historical export status remained `running`, so no historical success claim is promoted. Current source-faithful replay completed and regenerated four artifacts. In that toy objective, distributed recursive correction showed the strongest recovery.

**Run 2 — merged critique controls.** User: “You should be merging the 2 Sims. Apply the critique and run the sim again.” The successor added a common bounded-persistence layer, randomized-policy control, and ablations for memory, network coupling, and overcorrection penalty. Historical execution failed; current 90-second replay also timed out. The failure is preserved and directly motivates the optimized successor.

**Run 3 — optimized merged model.** Same user authorization inherited from Run 2. The assistant explicitly reduced execution overhead while preserving controls, ablations, shocks, and bounded persistence. Historical status: success. Current replay: success. `distributed_full` and closely related distributed ablations outranked centralized, random-policy, and laissez-faire modes in the constructed flourishing/resilience score.

**Run 4 — static-floor correction.** User: “Well remember we had solved that by amplifying static.” The successor reintroduced the previously solved bounded static-floor amplification rather than runaway persistence. Historical status: success. Current replay: success. The distributed family remained highest in the model, while the static floor changed recovery/persistence behavior without reversing the ordering.## Validation interpretation

Run 1 demonstrates a crucial provenance distinction: the export froze its status as `running`, while a later source-faithful replay completes. The later completion is new execution evidence and does not retroactively convert the historical node into a historical success.

Run 2 is a preserved negative-control/resource-bound branch. Its failure is part of the causal history, not an error to delete.

Runs 3 and 4 confirm that the preserved code executes today and reproduces the intended policy-comparison architecture. They do not establish that distributed recursive governance is superior in the real world because the scoring function, shocks, update rules, and target criteria are synthetic and theory-shaped.

The appropriate successor test is prediction-first and externally constrained: preregister policy behaviors, use independent or empirical outcome data, include matched-complexity alternatives, and prohibit post-result retuning.