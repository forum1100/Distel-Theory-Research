# DT Black-Hole / Recursive-Learning Run History

Status: `RECOVERED_CAUSAL_HISTORY / EXECUTION_CELLS_PRESERVED`

This ledger preserves the trip, not only the best endpoint. Each run records the problem it exposed and the rule introduced next. Short user continuations are interpreted with the immediately preceding assistant proposal as causal context; the short message itself is never rewritten into an invented instruction.

## Runs 1–4 — establish tests, recursive learning, and failed singularity push

1. **Black-hole prediction proxy suite** — 10 claims were converted into toy/proxy criteria. Historical summary: 7/10 proxy-survived, 3 were weak/failed. This was a screening exercise, not GR validation.
2. **Recursive cross-domain correction** — failures were allowed to apply corrective pressure across branches until correction magnitude approached exhaustion. Historical run converged after 38 interactions.
3. **Second-generation rerun from learned state** — starting already near convergence slightly worsened the endpoint (`0.019195` to `0.019336`), exposing over-refinement/oscillation risk instead of automatic improvement.
4. **DT singularity attempt** — explicitly failed: singularity flag false, peak singularity index about `0.143`, mean error worsened from about `0.0193` to `0.0244`. The attempted global push overcorrected.## Runs 5–9 — self-realization, restart, selective reopening, refinement, microstructure

5. **Self-realization loop** — each cycle tested, extracted a realization, applied it, and reran until 11 consecutive no-change events. The surviving lesson was that correction strength must weaken near convergence.
6. **Restart from run-1 conditions with learned rules preloaded** — added adaptive weakening plus rollback/preservation lock. Error improved from about `0.2079` to `0.0462`, but the run took 99 iterations and did not recover the earlier deep corridor.
7. **Selective reopening** — preserved viable branches while reopening only those stuck near the boundary. Improved about `0.0464` to `0.0413`; useful but still too shallow.
8. **Shared edge-error refinement** — the preceding assistant proposal specified coordinated correction of branches stuck around `0.04`; the user's short continuation accepted that proposed next run. Result improved about `0.0413` to `0.0237`.
9. **Domain-specific microstructure refinement** — after shared error was reduced, direct domain micro-corrections replaced further global correction. Mean error improved about `0.0237` to `0.0176`, with all branches below `0.02`.## Runs 10–14 — feature locking, covariance, autonomous continuation, Prism failure and repair

10. **Domain equation locking** — once all branches were below `0.02`, correction focused only on the highest-error feature with rollback if any branch left viability. Error improved about `0.0176` to `0.0111`; all eight branches entered the `0.015` band.
11. **Cross-feature covariance correction** — the preceding assistant proposal explicitly predicted that remaining error should live in feature relationships rather than isolated features. The user's continuation accepted that next run. Error improved about `0.0111` to `0.00262`.
12. **Autonomous continuation** — iteration count was no longer fixed; internal improvement/structural-error signals decided continuation. The run did not reach a no-change stop before the safety cap, but final mean error reached about `0.001195`. The safety-cap non-stop is preserved as a residual, not hidden as success.
13. **Prism from the beginning — FAILED EXECUTION** — the first full comparison was computationally too heavy and ended with `KeyboardInterrupt` before reliable files were produced. This is a first-class failed run.
14. **Prism from the beginning — lighter vectorized rerun** — changed the implementation specifically because run 13 was too heavy. Historical summary reported mean error improving about `0.02786` to `0.01203`, with fewer rollbacks. This does not erase the failed heavy implementation.## Runs 15–18 — Prism timing failure, compressed rerun, physical gates, inheritance test

15. **Prism injection timing sweep — FAILED EXECUTION** — the full phase sweep again ended in `KeyboardInterrupt`. The contemporaneous assistant response identified the sweep as still too heavy and proposed fewer trials/fewer cycles while preserving the same timing question.
16. **Compressed Prism timing rerun** — the faster phase-threshold model completed. Historical ranking placed singular/mid/coarse/immediate phases in that order, with the derived practical interpretation that Prism works best after viable structure exists but before fragile ultra-refinement dominates.
17. **Propulsion/4D hard-gate integration** — the user's short continuation followed an assistant proposal to reintegrate conservation, causality, wake/heat, nonzero detectability, and homecoming overlap as hard validity gates. The run favored hard-gating rather than optimizing those terms as goals.
18. **Convergence-inheritance test** — compared no inheritance, fact-only memory, and convergence-topology inheritance. The first version did not prove the claim: inherited topology reduced correction waste but became too conservative and failed deep convergence within the cycle cap. The next identified requirement was an explicit reopening rule.

## Causal interpretation rule
For short responses such as `go ahead`, `continue`, or equivalent acknowledgments, the immediately preceding assistant proposal is retained as the action context. This is a derived edge between two verbatim turns, not permission to invent additional user instructions.

## Scientific status
These runs are toy/meta-simulations spanning black-hole proxy criteria and cross-domain correction architecture. A proxy survival is not evidence that GR/QFT has been reproduced. Failed runs, timeout/interrupt conditions, parameter changes, rollback rules, and stopping failures are part of the evidence and must remain replayable alongside successful branches.